{
   "version": "2.0.0",
   "tasks": [
      {
         "label": "makeall",
         "type": "shell",
         "options": {
            "cwd": "${workspaceRoot}/build"
         },
         "command": "cmake ${workspaceRoot} ; make",
         "group": {
            "kind": "build",
            "isDefault": true
         }
      }
   ]
 }
